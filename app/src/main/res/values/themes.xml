<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using AppCompat -->
    <style name="Theme.EasyChat" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/my_primary</item>
        <item name="colorPrimaryDark">@color/my_primary</item> <!-- AppCompat dùng colorPrimaryDark -->
        <item name="colorAccent">@color/my_secondary</item>   <!-- AppCompat dùng colorAccent -->
        <item name="android:windowBackground">@color/off_white</item>
    </style>
</resources>
